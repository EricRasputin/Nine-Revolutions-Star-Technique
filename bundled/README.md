# Bundled Ebooks

This folder contains compiled ebook versions of the translated chapters.

-   `epubs/` — EPUB format (for e-readers, phones, tablets)
-   `txts/` — Plain text format

These are generated from the chapter files in `translations/` using the volume definitions in `assets/sep.json`.
